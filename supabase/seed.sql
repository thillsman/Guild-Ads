-- Seed data for local development
-- This runs after migrations when you run `supabase db reset`.
-- The current local reset path intentionally leaves the database empty so the
-- schema can be reset safely even as the live data model evolves.
--
-- Create test users, apps, campaigns, and bookings through the dashboard or
-- via targeted SQL inserts as needed for the scenario you are testing.
select 1;
